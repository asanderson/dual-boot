# versions.env — pinned versions for repeatable installs.
# Bump deliberately, then re-run the affected step.

# OpenCore Legacy Patcher release used for the macOS Sequoia upgrade
# (https://github.com/dortania/OpenCore-Legacy-Patcher/releases).
# 2.4.1 targets macOS 15.x Sequoia; check the releases page before major
# macOS point updates.
OCLP_VERSION="2.4.1"
